import React from "react";
import PortalApp from "../web/src/PortalApp";

/** Portal Tecnotics — React directo en Expo/Metro, sin WebView. */
export default function IndexScreen() {
  return <PortalApp />;
}