import BillingScreenHostNative from "../../billing/BillingScreenHost.native";

/** Facturar nativo = SDK @tecnotics/fe-billing-native (mismo back que el portal). */
export default function FacturarNative() {
  return <BillingScreenHostNative />;
}
