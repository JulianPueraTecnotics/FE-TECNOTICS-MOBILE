import { Text, View } from "react-native";

/** Firma PDF solo disponible en web (canvas/DOM). */
export default function PDFSignatureNewNative() {
  return (
    <View>
      <Text>La firma de PDF está disponible en la versión web del portal.</Text>
    </View>
  );
}
