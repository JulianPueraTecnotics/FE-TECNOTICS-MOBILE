/** Altura del header autenticado nativo (sin safe area). */
export const AUTH_HEADER_HEIGHT = 56;

/** Altura de la barra inferior de navegación (sin safe area). */
export const BOTTOM_NAV_HEIGHT = 58;

export { PUBLIC_HEADER_HEIGHT } from "./publicShell.constants";
